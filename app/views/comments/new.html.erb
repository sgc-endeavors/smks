<h1>Create a Comment</h1>



<%= form_for(@new_comment, url: comments_path(@new_comment)) do |f| %>
	<%= f.label :body, "Comment", for: "body" %>
	<%= f.text_area :body, id: "body" %><br>

	<%= f.hidden_field :story_id, value: @story_id%>

	<%= submit_tag "Submit" %>

<%end%>

